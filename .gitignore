# Ambiente Python
.venv/
__pycache__/
*.pyc
.idea/
.claude/
.DS_Store
old/
# === Pesos dos adapters ===
# Nenhum peso entra no git: o adapter do 27B (~450MB) passa do limite de 100MB do GitHub.
# Os adapters 9B antigos (v9/v10/v11b) continuam acessíveis pelo histórico.
*.safetensors
*.gguf

# Runtime / temporários
*.log
**/screenshot_*.png
2_interface/back/conversas.db*

# Manual de operação pessoal (comandos, custos, pipeline de treino) + mapa
/COMANDOS.md
/MAPA.md

# Material didático/pessoal do Victor (não sobe — é pra estudo dele, não pro mundo)
# docs/ = os DOCX + geradores; README.md por pasta = guias internos.
# O !/README.md reativa SÓ o README da raiz (o cartão de visita do repo).
docs/
**/README.md
!/README.md

# Treino + laboratório = proprietários (o "molho" da ADA — não entra na vitrine)
4_treino/
5_lab/

# Benchmark inteiro fica local (itens com pessoas reais, respostas, avaliações); o resultado mais recente vai no README
6_benchmark/
_modelo/*_fp32/

# Personas com dados pessoais reais (privacidade de terceiros — uso local)
1_ada/personas_local.py
# IPs de quem é quem na interface (Tailscale) — uso local
.env

# Andaime de pronome (gambiarra temporária até o v12 aprender; não é produto)
1_ada/conhecimento/base/quem_e_quem.md
