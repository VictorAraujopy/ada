# Ambiente Python
.venv/
__pycache__/
*.pyc
.idea/
.claude/
.DS_Store

# === Pesos dos adapters ===
# Só o adapter ATUAL (v10, ~83M) entra — é o que faz a ADA funcionar.
*.safetensors
!_modelo/ada_v10_9b/adapters.safetensors
# Versões antigas fora do GitHub
_modelo/ada_v9_9b/
_modelo/ada_v11_a16_9b/
_modelo/ada_v11b_a16_9b/

# Voz pessoal (referência de clonagem) e áudios temporários
_arquivado/4_voz/voz_ada.wav
_arquivado/4_voz/_fala_*.wav

# Runtime / temporários
*.log
**/screenshot_*.png
2_interface/back/conversas.db*

# Manual de operação pessoal (comandos, custos, pipeline de treino) + mapa
/COMANDOS.md
/MAPA.md

# Material didático/pessoal do Victor (não sobe — é pra estudo dele, não pro mundo)
# docs/ = os DOCX + geradores; README.md por pasta = guias internos.
# O !/README.md reativa SÓ o README da raiz (o cartão de visita do repo).
docs/
**/README.md
!/README.md

# Voz/daemon descontinuados (codigo arquivado fica no repo; pesados pro Mac atual)


# Treino + laboratório = proprietários (o "molho" da ADA — não entra na vitrine)
4_treino/
5_lab/

# Benchmark: o harness (scripts + perguntas) é público; os RESULTADOS são pessoais
6_benchmark/resultados/
