# Dependências da ADA — IA pessoal local (Mac, Apple Silicon / Metal). Python 3.12.
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

# --- Cérebro + runtime (rodar a ADA no Mac) ---
llama-cpp-python==0.3.35 # roda o Qwen3.8-27B em GGUF 3 bits na GPU (compila com Metal)
huggingface_hub==1.17.0  # baixa o GGUF e o tokenizer
mlx-vlm==0.5.0          # ADA antiga (Qwen3.5-9B em MLX) — benchmarks de comparação
transformers==5.9.0     # tokenizer e chat template do Qwen
numpy==2.4.6

# --- Interface web (2_interface — só pra demo) ---
fastapi==0.136.3
uvicorn==0.48.0
